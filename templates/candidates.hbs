{{#each matches}}
	<div class="row">
		<div class="col-sm-6 col-md-4">
			<div class="thumbnail">
			 	<img src="{{ image }}" alt="...">
				<div class="caption">
					<h3>{{ name }}</h3>
					<section>{{ bio }}</section>
					<p><a href="#" class="btn btn-primary" role="button">Favorite</a></p>
				</div>
			</div>
		</div>
	</div>
{{/each}}
		